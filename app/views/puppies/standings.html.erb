<h3>Current standings:</h3>
<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Puppy</th>
      <th>Rating</th>
    </tr>
  </thead>

  <tbody>
    <% @puppies.each do |puppy| %>
      <tr class="standing-row">
        <td><p class="standing-id"><%= puppy.id %></p></td>
        <td><img class="standing-pic" src="<%= puppy.path %>"/></td>
        <td><p class="standing-number"><%= number_with_precision(puppy.rating, :precision => 2) %></p></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Seen these all before? Get more puppies!', more_path %>
<br/>
<%= link_to 'Back', puppies_path %>

<br>
